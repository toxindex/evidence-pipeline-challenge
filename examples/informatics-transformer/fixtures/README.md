# Fixture contract

Add a small, redistributable subset extracted from the Tox21 Kiln brick. Record
the exact query, brick `built_at`, source, license, row identities and checksum.
The public starter intentionally omits the data so the candidate must establish
that provenance rather than inherit unexplained rows.

